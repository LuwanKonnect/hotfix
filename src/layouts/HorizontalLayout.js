// !Do not remove the Layout import
import Layout from '@layouts/HorizontalLayout'

const HorizontalLayout = props => <Layout footer='@ ATOD 2021'  {...props}>{props.children}</Layout>

export default HorizontalLayout
