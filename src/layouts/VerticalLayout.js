// !Do not remove the Layout import
import Layout from '@layouts/VerticalLayout'

const VerticalLayout = props => <Layout footer='@ ATOD 2021'  {...props}>{props.children}</Layout>

export default VerticalLayout
